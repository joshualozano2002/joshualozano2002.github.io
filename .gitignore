node_modules
dist
.vite-react-ssg-temp
.DS_Store
*.local
.env
.env.*
!.env.example

# Previous create-react-app site, kept on disk during the rebuild.
# Delete the folder once you are happy with the new one; the tracked files
# are still recoverable from commit d2af8ac.
legacy/
